# AGENTS.md

## Cursor Cloud specific instructions

This repository (`AaztraAI`) is currently an empty scaffold with only a `README.md`. There is no application code, no build system, no dependencies, and no services to run.

### Current state

- Single file: `README.md`
- No languages, frameworks, or package managers configured
- No tests, linters, or build steps

### For future agents

Once application code is added, update this section with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`)
- How to run the dev server and any required auxiliary services
- How to run linting, tests, and builds
- Any non-obvious caveats discovered during setup
